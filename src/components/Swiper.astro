<!-- Swiper JS -->
<script
  is:inline
  src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

<!-- Initialize Swiper -->
<script is:inline>
  var swiper = new Swiper(".mySwiper", {
    effect: "fade",
    navigation: {
      nextEl: ".swiper-button-next",
      prevEl: ".swiper-button-prev",
    },
    pagination: {
      el: ".swiper-pagination",
    },
  });

  // Color exterior
  const extSwiper = new Swiper(".extSwiper", {
    effect: "fade",
  });

  function changeColor(index) {
    extSwiper.slideTo(index);
  }

  // Color interior
  const intSwiper = new Swiper(".intSwiper", {
    effect: "fade",
  });

  function changeColorInt(index) {
    intSwiper.slideTo(index);
  }
</script>

<style lang="scss" is:global>
  .swiper-button-next,
  .swiper-button-prev {
    width: 38px;
    height: 38px;
    top: 140px;
    @media (min-width: 1280px) {
      top: 47%;
    }
  }
  .leftSwiper .swiper-button-next {
    @media (min-width: 1280px) {
      left: 488px;
    }
  }
  .swiper-button-next:after,
  .swiper-button-prev:after {
    content: "";
    background-image: url("/emzoom-rstyle/img/flecha-izq.svg");
    display: block;
    width: 38px;
    height: 38px;
  }
  .swiper-button-next:after {
    background-image: url("/emzoom-rstyle/img/flecha-der.svg");
  }
  .swiper-horizontal > .swiper-pagination-bullets {
    @media (min-width: 1280px) {
      bottom: 56px;
      width: 534px;
    }
  }
  .swiper-pagination-bullet-active {
    background: #ffffff;
  }

  .mySwiper .swiper-slide {
    background-color: black;
    height: auto;
  }

  /* Right swiper */
  @media (min-width: 1280px) {
    .rightSwiper {
      .swiper-button-next,
      .swiper-button-prev {
        top: 40%;
      }
      .swiper-button-prev {
        right: 488px;
        left: auto;
      }
      &.swiper-horizontal > .swiper-pagination-bullets {
        right: 0;
        left: auto;
        bottom: 103px;
      }
    }
  }

  /* Swiper negro */
  .leftSwiper2,
  .rightSwiper2 {
    .swiper-button-next:after {
      background-image: url("/emzoom-rstyle/img/flecha-der-n.svg");
    }
    .swiper-button-prev:after {
      background-image: url("/emzoom-rstyle/img/flecha-izq-n.svg");
    }
    &.swiper-horizontal > .swiper-pagination-bullets {
      bottom: 12px;
      @media (min-width: 1280px) {
        bottom: 32px;
        width: 527px;
      }
    }
    .swiper-pagination-bullet {
      opacity: 1;
      width: 6px;
      height: 6px;
    }
    .swiper-pagination-bullet-active {
      background: #000000;
      transform: scale(1.7);
    }
    .swiper-button-next,
    .swiper-button-prev {
      top: auto;
      bottom: 0;
      @media (min-width: 1280px) {
        bottom: 24px;
      }
    }
  }
  .leftSwiper2 {
    &.swiper-horizontal > .swiper-pagination-bullets {
      right: 0;
      left: auto;
    }
    .swiper-button-prev {
      left: 50px;
      @media (min-width: 1280px) {
        left: auto;
        right: 436px;
      }
    }
    .swiper-button-next {
      right: 50px;
    }
  }
  .rightSwiper2 {
    .swiper-button-prev {
      left: 50px;
    }
    .swiper-button-next {
      right: 50px;
      @media (min-width: 1280px) {
        right: auto;
        left: 436px;
      }
    }
  }
</style>
